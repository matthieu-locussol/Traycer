SET( APP_TARGET "TraycerTests" )

FILE( GLOB_RECURSE APP_SOURCES main.cpp )

ADD_EXECUTABLE(
    ${APP_TARGET}
    ${APP_SOURCES}
    ${APP_HEADERS}
    ${APP_INLINES}
    ${TRAYCER_FILES}
)

FILE( COPY "${PROJECT_SOURCE_DIR}/References" DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/." )